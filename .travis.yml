language: node_js
node_js:
  - "node"
  - "7"
cache:
  directories:
    - "node_modules"
stages:
  - test
  - build
  - deploy
jobs:
  include:
    - stage: test
      script: npm test
    - stage: build
      script: npm build
    - stage: deploy
      script: skip
      deploy:
        provider: pages
        skip-cleanup: true
        github-token: $GITHUB_TOKEN
        keep-history: true
        local-dir: build
        target-branch: master
        on:
          branch: develop
